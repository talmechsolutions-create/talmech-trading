import Link from 'next/link';import {scrap,formatINR} from '@/lib/data';export const metadata={title:'Scrap Metal Marketplace'};export default function Scrap(){return <main className="container section"><span className="eyebrow">Grade-wise scrap trading</span><h1 className="pageTitle">Scrap Metal Marketplace</h1><p className="lead">Buy and sell steel, copper, aluminum, brass and mixed scrap with regional demand visibility. Use the funnel page to coordinate your own scrap supplier network.</p><div style={{margin:'20px 0'}}><Link className="btn" href="/scrap-funnel">Open Scrap Supplier Funnel</Link></div><div className="grid cards4">{scrap.map(s=><div className="card" key={s[0]}><span className="badge">{s[2]}</span><h2>{s[0]}</h2><p className="muted">{s[1]}</p><p>{s[5]}</p><p className="price">{formatINR(Number(s[4]))}/MT</p><p>Available: {s[3]}</p><Link className="btn secondary" href="/scrap-funnel">Add / Contact Supplier</Link></div>)}</div></main>}
