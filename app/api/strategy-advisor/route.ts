import {NextResponse} from 'next/server';import {metalBusinessProfiles} from '@/lib/strategyKnowledge';
function fallback({metal,city,role,capital,qty}:any){const p=metalBusinessProfiles.find(x=>x.metal===metal)||metalBusinessProfiles[0];return{summary:`Target ${metal} in ${city||'selected city'} with a no-inventory brokerage model. Capture demand first, verify supplier second, calculate logistics third, and quote with clear commission. Capital mode: ${capital||'low budget'}. Quantity focus: ${qty||'small to medium lots'}.`,topActions:[`Build a list of ${p.bestBuyers.slice(0,4).join(', ')} in ${city||'your target region'}.`,`Call buyers and capture grade, form, size, quantity, target price, delivery city and payment terms.`,`Call suppliers only after demand is clear; ask for stock proof, GST, grade proof, loading point and quote validity.`,`Use logistics planner before final quote and add freight, loading/unloading, toll, risk buffer and Talmech commission.`,`Follow up using CRM stages and close small repeatable transactions before going into large deals.`],leadSearches:p.searchKeywords.map((k:string)=>`${k} in ${city||'India industrial area'}`),risks:p.riskSignals.concat(['credit risk','transporter delay','price validity expiry']).slice(0,6),pitch:`Hello, this is Talmech Trading. We connect verified ${metal} buyers, suppliers and transporters for ${p.products.slice(0,4).join(', ')}. We can help with market-rate comparison, sourcing, buyer/supplier coordination and city-to-city logistics. Can I understand your current ${role==='supplier'?'available stock':'requirement'}?`,marginPlan:'Start with commission/service margin instead of inventory. Final quote = supplier price + logistics + loading/unloading + statutory cost + risk buffer + Talmech margin. Lock quote validity in writing.'}}
export async function POST(req:Request){const body=await req.json().catch(()=>({}));const key=process.env.OPENAI_API_KEY;if(!key)return NextResponse.json({mode:'rule-based',advice:fallback(body)});try{const prompt=`Return ONLY valid JSON with keys summary, topActions array, leadSearches array, risks array, pitch, marginPlan. Create practical Indian metal brokerage strategy. Inputs: ${JSON.stringify(body)}. Business has low budget, no inventory, wants demand-supply matching, logistics commission, cold calls and CRM followup.`;const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{'Authorization':`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({model:'gpt-4.1-mini',input:prompt,temperature:.4})});const j=await r.json();const txt=j.output_text||j.output?.[0]?.content?.[0]?.text||'';const advice=JSON.parse(txt);return NextResponse.json({mode:'openai',advice});}catch{return NextResponse.json({mode:'fallback',advice:fallback(body)});}}
